# from django.urls import path, include
# from rest_framework import routers

# from blog.views import (
#     UserViewSet,
#     CategoryViewSet,
#     ArticleViewSet,
# )

# router = routers.DefaultRouter()
# router.register("user/<str:query>/", UserViewSet)
# router.register("category/<str:query>/", CategoryViewSet)
# router.register("article/<str:query>/", ArticleViewSet)

# urlpatterns = [
#     path("", include(router.urls))
# ]